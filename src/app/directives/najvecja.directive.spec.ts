import { NajvecjaDirective } from './najvecja.directive';

describe('NajvecjaDirective', () => {
  it('should create an instance', () => {
    const directive = new NajvecjaDirective();
    expect(directive).toBeTruthy();
  });
});
